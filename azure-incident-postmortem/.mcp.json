{
  "mcpServers": {
    "azure-devops": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@azure-devops/mcp", "${user_config.azure_devops_org}"],
      "env": {
        "AZURE_DEVOPS_PAT": "${user_config.azure_devops_pat}"
      }
    }
  }
}
